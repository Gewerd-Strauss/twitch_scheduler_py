# .pre-commit-config.yaml
repos:
  - repo: https://github.com/psf/black
    rev: 24.10.0  # Specify the version of Black you want to use
    hooks:
      - id: black
        language_version: python3  # or specify your Python version
